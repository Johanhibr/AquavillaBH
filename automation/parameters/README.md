# Introduction 
This is the folder storing the parameter file used when deploying Fabric Items using the fabric-cicd Python library. 
The folder can also contain environment property files which are used when enhancing the paramenter file when running deployment locally or through Azure DevOps or GitHub.