# SQL analytics endpoint

The endpoint can be used to expose data in a relational format to Business Analyst and/or applications. Typically only on the Curated layer, but in some cases it can also be valuable to give access so a few users can help validate and test data in the Base layer.

